apiVersion: v1
kind: Service
metadata:
  name: tyhewnvf
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "tyhewnvf"
    port: 8080
  selector:
    app: tyhewnvf
